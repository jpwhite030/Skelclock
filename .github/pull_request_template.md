## What this changes

<!-- One or two sentences, in plain terms. What can someone do now that they
     could not before, or what was broken that now works? -->

## How it was checked

<!-- What did you actually run or click? "npm test passes" is not the same as
     "I clocked on and off on a real handset and watched it sync." -->

- [ ] `npm test` passes locally
- [ ] `npm run poc` still passes
- [ ] Tried it in the app / dashboard, not only in tests

## Anything to watch

<!-- Anything risky, unfinished, or that needs a decision. Say so here rather
     than leaving it to be discovered. Write "nothing" if there is nothing. -->

---

<!-- If this touches any of the following, say why in the section above:
       supabase/migrations   changes the database shape
       packages/odoo         writes to the system payroll runs from
       sync.ts / approval.ts a bug here means shifts quietly miss payroll
     None of these are off limits — they just need a sentence of explanation. -->
